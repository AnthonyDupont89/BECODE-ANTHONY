console.log(numArray);
    
    numArray.forEach(num => console.log(num*10));
    console.log(numArray);

    numArray = numArray.map(num => num*10);
    console.log(numArray);

    numArray = numArray.map(num => num*10);
    console.log(numArray);